#include <stdio.>

/**
 * main - prints "prints the size of various types on the computer it
 *  is compiled and run on."
 *  Return: always 0.
 */
int main(void)

{
printf("size ofan int: %d byte(s) \n, sizeof(int));
		return (0);
}
